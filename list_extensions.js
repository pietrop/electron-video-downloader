/**
 * To add alist of supported sites. 
 * commenting out for now, as eg vimeo doesn't seem to work.
 */
// var youtubedl = require('youtube-dl');

// var listOfExtractorsElement = document.getElementById('listOfExtractors');

// function setElementlistOfExtractors(extractors){
// 	listOfExtractorsElement.innerHTML += extractors; 
// }

// youtubedl.getExtractors(true, function(err, list) {
// // setElementlistOfExtractors+=list[i]+'<br>'
//   console.log('Found ' + list.length + ' extractors');
//   for (var i = 0; i < list.length; i++) {
//   	setElementlistOfExtractors(list[i]+'<br>');
//     console.log(list[i]);
//   }
// });